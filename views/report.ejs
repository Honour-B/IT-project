<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Monthly Report</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
 <style>
  body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: linear-gradient(to right, #e0f7fa, #f1f8ff);
    min-height: 100vh;
  }

  .container {
    max-width: 800px;
  }

  .card-custom {
    border: none;
    border-radius: 20px;
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
    overflow: hidden;
    background-color: #ffffff;
  }

  .card-header {
    padding: 30px 20px;
    background: linear-gradient(135deg, #007bff, #0056b3);
    color: white;
    border-bottom: none;
    border-radius: 20px 20px 0 0;
    position: relative;
  }

  .card-header h3 {
    font-size: 1.6rem;
    font-weight: bold;
  }

  .card-header .btn {
    position: absolute;
    right: 20px;
    top: 25px;
    background-color: #ffffff;
    color: #007bff;
    border-radius: 50px;
    transition: all 0.3s ease-in-out;
  }

  .card-header .btn:hover {
    background-color: #e9ecef;
  }

  .section-heading {
    font-weight: 600;
    font-size: 1.1rem;
    color: #333;
    margin-bottom: 10px;
  }

  textarea.form-control {
    border-radius: 10px;
    box-shadow: none;
    transition: box-shadow 0.3s ease;
  }

  textarea.form-control:focus {
    border-color: #007bff;
    box-shadow: 0 0 0 0.2rem rgba(0,123,255,0.2);
  }

  .btn-success {
    border-radius: 50px;
    padding: 10px 30px;
    font-weight: 600;
  }

  .alert {
    border-radius: 10px;
    font-size: 0.95rem;
  }

  .text-center {
    margin-top: 10px;
  }
</style>

</head>
<body class="bg-light">

  <div class="container py-5">
    <div class="card card-custom">
      <div class="card-header bg-primary text-white text-center rounded-top-4">
        <h3 class="mb-0">📅 Monthly Report for <%= currentMonthYearFormatted %></h3>
         <a href="/submit-log" class="btn btn-light btn-sm">🏠 Home</a>
      </div>

      <% if (submitted) { %>
  <div class="alert alert-success text-center">
    ✅ Your monthly report was successfully submitted.
  </div>
<% } %>

<% if (alreadySubmitted) { %>
  <div class="alert alert-info text-center">
    ✅ You have already submitted the report for this month.
  </div>
  <div class="text-center">
  </div>

<% } else if (canSubmit) { %>
        <div class="card-body">

          <form action="/monthly-report" method="POST">
            <input type="hidden" name="month_year" value="<%= currentMonthYear %>">

            <div class="section-heading">🟥 Problems Encountered</div>
            <div class="mb-4">
              <textarea name="problems_encountered" class="form-control" rows="5" required
                placeholder="Describe any challenges you encountered during the month..."></textarea>
            </div>

            <div class="section-heading">🟩 Solutions Provided</div>
            <div class="mb-4">
              <textarea name="solutions_provided" class="form-control" rows="5" required
                placeholder="Explain how you tackled or resolved the issues..."></textarea>
            </div>

            <div class="text-center">
              <button type="submit" class="btn btn-success px-5">✅ Submit Report</button>
            </div>
          </form>

        </div>
      <% } else { %>
        <div class="card-body text-center">
          <div class="alert alert-warning">
            ⚠️ Monthly reports can only be submitted during the <strong>last week</strong> of each month.
          </div>
        </div>
      <% } %>
    </div>
  </div>

</body>
</html>
