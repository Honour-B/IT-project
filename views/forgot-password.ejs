<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Forgot Password</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(to right, #e3f2fd, #bbdefb);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .card {
      border: none;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
      border-radius: 1rem;
    }

    .card-title {
      font-weight: 600;
    }

    .form-control {
      border-radius: 0.5rem;
    }

    .btn-primary {
      background-color: #1976d2;
      border: none;
      border-radius: 0.5rem;
    }

    .btn-primary:hover {
      background-color: #0d47a1;
    }

    .alert {
      border-radius: 0.5rem;
    }

    a {
      color: #1976d2;
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }

    .container {
      padding-top: 50px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="card mx-auto" style="max-width: 450px;">
      <div class="card-body">
        <h4 class="card-title text-center mb-4">🔐 Forgot Password</h4>

        <% if (error) { %>
          <div class="alert alert-danger"><%= error %></div>
        <% } %>

        <form action="/forgot-password" method="POST">
          <div class="mb-3">
            <label for="matric_num" class="form-label">Enter Matric Number</label>
            <input type="text" name="matric_num" id="matric_num" class="form-control" required>
          </div>

          <button type="submit" class="btn btn-primary w-100">Verify & Send OTP</button>
        </form>

        <div class="mt-3 text-center">
          <a href="/login">← Back to Login</a>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
