<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Calendar Log</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(135deg, #e9f5ff, #ffffff);
      padding: 2em;
      font-family: 'Segoe UI', sans-serif;
      color: #333;
    }

    .back-btn {
      margin-bottom: 20px;
      padding: 10px 20px;
      background-color: #0d6efd;
      color: #fff;
      text-decoration: none;
      border-radius: 8px;
      font-weight: 500;
      display: inline-block;
      transition: background-color 0.3s ease;
    }

    .back-btn:hover {
      background-color: #084298;
    }

    .calendar-grid {
      display: grid;
      grid-template-columns: repeat(7, 1fr);
      gap: 12px;
      max-width: 900px;
      margin: 0 auto;
    }

    .day-box {
      background-color: #ffffff;
      border: 1px solid #dee2e6;
      border-radius: 10px;
      padding: 15px 10px;
      min-height: 100px;
      text-align: center;
      font-weight: 500;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
      cursor: pointer;
      position: relative;
      transition: all 0.3s ease;
    }

    .day-box:hover {
      transform: translateY(-3px);
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
      background-color: #f1faff;
    }

    /* State styles */
    .submitted {
      background-color: #d1e7dd !important;
      border-left: 5px solid #198754;
    }

    .missed {
      background-color: #f8d7da !important;
      border-left: 5px solid #dc3545;
    }

    .future {
      background-color: #fefefe !important;
      opacity: 0.85;
    }

    /* Today highlight */
    .today {
      border: 2px solid #0d6efd !important;
      box-shadow: 0 0 0 3px #cfe2ff;
    }

    .calendar-container {
      background-color: #ffffff;
      border-radius: 15px;
      padding: 30px;
      max-width: 960px;
      margin: 0 auto;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.07);
      border: 1px solid #dee2e6;
    }

    .day-box {
      padding: 10px;
      border-radius: 6px;
      border: 1px solid #ccc;
      text-align: center;
      cursor: pointer;
      transition: 0.3s;
    }

    .day-box.submitted {
      background-color: #d1e7dd !important;
      /* green */
      border: 2px solid #0f5132;
      /* optional green border */
      color: #0f5132;
    }

    .day-box.missed {
      background-color: #f8d7da !important;
      /* red */
    }

    .day-box.future {
      background-color: #f0f0f0 !important;
      /* white-ish */
    }

    .day-box.today {
      border: 2px solid #0d6efd;
      /* just outline, no background override */
    }

    .btn-home {
      background-color: #0d6efd;
      color: white;
      padding: 10px 18px;
      border-radius: 8px;
      font-weight: 500;
      text-decoration: none;
      display: inline-block;
      margin-bottom: 20px;
    }

    .btn-home:hover {
      background-color: #084298;
    }

    .day-box.disabled {
      pointer-events: none;
      opacity: 0.6;
      cursor: not-allowed;
    }

    .day-box.invisible {
      visibility: hidden;
      pointer-events: none;
    }
  </style>
</head>

<body>
  <div class="text-center mb-4">
    <a href="/submit-log" class="btn-home">
      <i class="fas fa-arrow-left"></i> Home
    </a>
  </div>
  <a href="javascript:history.back()" class="btn btn-secondary back-btn">← Back</a>

  <h3 class="mb-4">
    <!-- <%= new Date(currentYear, currentMonth - 1).toLocaleString('default', { month: 'long', year: 'numeric' }) %> -->
  </h3>

  <div class="d-flex justify-content-between align-items-center mb-3 calendar-container">
    <!-- <a href="/calendar?month=<%= currentMonth - 1 === 0 ? 12 : currentMonth - 1 %>&year=<%= currentMonth - 1 === 0 ? currentYear - 1 : currentYear %>" class="btn btn-outline-primary">&laquo; Previous</a> -->

    <h3 class="mb-0">
      <!-- <%= new Date(currentYear, currentMonth - 1).toLocaleString('default', { month: 'long', year: 'numeric' }) %> -->
    </h3>

    <!-- <a href="/calendar?month=<%= currentMonth + 1 === 13 ? 1 : currentMonth + 1 %>&year=<%= currentMonth + 1 === 13 ? currentYear + 1 : currentYear %>" class="btn btn-outline-primary">Next &raquo;</a> -->
  </div>


  <div class="calendar-grid mb-2 text-center fw-bold">
    <div>Sun</div>
    <div>Mon</div>
    <div>Tue</div>
    <div>Wed</div>
    <div>Thu</div>
    <div>Fri</div>
    <div>Sat</div>
  </div>

  <!-- <%

    const firstDay = new Date(currentYear, currentMonth - 1, 1).getDay();
    const lastDate = new Date(currentYear, currentMonth, 0).getDate(); // This fixes your error
    const todayStr = new Date().toISOString().split('T')[0];
    %>

  <div class="calendar-grid">
    <% 
      // const rawFirstDay = new Date(currentYear, currentMonth - 1, 1).getDay(); // 0 = Sunday, 1 = Monday ...
     // const firstDay = (rawFirstDay === 0) ? 6 : rawFirstDay - 1;

     // const lastDate = new Date(currentYear, currentMonth, 0).getDate();
     // const todayStr = new Date().toISOString().split('T')[0];

      for (let i = 0; i < firstDay; i++) 
     
      { %>
  
    <% } %>

    <% 
     for (let day = 1; day <= lastDate; day++)
    const dayOfWeek = new Date().getDay(); // today is used to find the current week
    const offset = (dayOfWeek === 0) ? 6 : dayOfWeek - 1;
    const startOfWeek = new Date();
    startOfWeek.setDate(new Date().getDate() - dayOfWeek);
    startOfWeek.setHours(0, 0, 0, 0);
    %>

    <% for (let day = 1; day <= lastDate; day++) {
      const dateObj = new Date(currentYear, currentMonth - 1, day);
     const pad = n => n.toString().padStart(2, '0');
     const dateKey = `${dateObj.getFullYear()}-${pad(dateObj.getMonth() + 1)}-${pad(dateObj.getDate())}`;

        const dayDate = new Date(currentYear, currentMonth - 1, day);
          const today = new Date();
          const weekStart = new Date(today);
          weekStart.setDate(today.getDate() - today.getDay());

         // Strip time
          dayDate.setHours(0,0,0,0);
          today.setHours(0,0,0,0);

          const isInCurrentWeek = dayDate >= weekStart && dayDate <= today;

      const log = logs[dateKey];
      const isToday = dateKey === todayStr;
      const isFuture = dateObj > new Date();
      const isInCurrentWeek = dateObj >= startOfWeek && dateObj <= new Date();

      let classes = "day-box";
      if (log) classes += " submitted";
      else if (dateObj <= new Date()) classes += " missed";
      else classes += " future";
      if (isToday) classes += " today";
      const isDisabled = !(isInCurrentWeek);
      if (isDisabled) classes += " disabled";

    %>
      <div class="<%= classes %>" data-date="<%= dateKey %>" data-day="<%= day %>" data-log='<%- JSON.stringify(log || {}) %>' onclick="openModal(this)">
        <strong><%= day %></strong>
      </div>
    <% } %>
  </div> -->

  <!-- Modal -->
  <div class="modal fade" id="logModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
      <form method="POST" action="/calendar" class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="modalDate">Log Entry</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
        </div>
        <div class="modal-body">
          <input type="hidden" name="log_id" id="logId">
          <input type="hidden" name="date" id="logDate">
          <textarea name="log_text" class="form-control" id="logText" rows="4" required></textarea>
        </div>
        <div class="modal-footer">
          <button type="submit" class="btn btn-primary">Save Log</button>
        </div>
      </form>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <!-- <script src="/calendarlog.js"></script> -->
</body>

</html>